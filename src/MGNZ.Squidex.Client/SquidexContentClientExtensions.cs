namespace MGNZ.Squidex.Client
{
  using System.Threading.Tasks;

  using MGNZ.Squidex.Client.Model;

  public static class SquidexContentClientExtensions
  {
    //public static async Task<ItemContent<TModel>> Create<TModel>(this ISquidexContentClient that, string application, string name, TModel model, bool publish = true)
    //{
    //  var response = await that.Create(application, name, model);
    //  if (publish) await that.Publish(application, name, response.Id);

    //  return response;
    //}
  }
}